= form_with(url: search_path, method: :get, local: true, class: 'search', html: { data: { type: "json" }}) do |f|
  = f.text_field :search, class: 'form-control'
  = f.select(:scope, options_for_select(%w(All Questions Answers Comments Users)))
  = f.submit 'Search', class: 'btn'
